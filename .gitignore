.DS_Store
__pycache__/
*.pyc
*.pyo
*.pyd
.matplotlib/

# Local virtual envs / editor noise
.venv/
venv/
.python-version
.env
.env.*

# Logs and scratch
*.log
tmp/
archive/

# Keep the submission repo focused on the runtime app, not experiments
quantile_model/experiments/

# Ignore alternate quantile artifacts that are not part of the final app path
quantile_model/artifacts/*
!quantile_model/artifacts/points_ou_quantile_artifact_split.pt

# Ignore diagnostics and generated analysis tables from the alternate-lines pipeline
multi_output_threshold_mlp/diagnostics/
multi_output_threshold_mlp/*_predictions.csv
multi_output_threshold_mlp/walk_forward_stacked_pair_type_summary.csv

# Keep `data/` in the repo by default; only exclude the local helper file below.
data/game_event_info.csv
data/team_logos/
